#cloud-config
package_update: true
package_upgrade: false

runcmd:
  - |
    curl -sfL https://get.k3s.io | \
      K3S_URL="https://${control_plane_ip}:6443" \
      K3S_TOKEN="${k3s_token}" \
      sh -s - agent